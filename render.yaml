name: my-node-app
env:
  NODE_ENV: production
build:
  command: npm ci && npm run build
  publish: ./build
start:
  command: node dist/server.js   # Use the correct command to run the transpiled server.js
